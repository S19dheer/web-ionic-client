'use strict';

/**
 * @ngdoc function
 * @name crudWebClientApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the crudWebClientApp
 */

angular.module('crudWebClientApp').controller('MainCtrl', function($scope) {
    //debugger;
    $scope.awesomeThings = [
        'HTML5 Boilerplate',
        'AngularJS',
        'Karma'
    ];
});
